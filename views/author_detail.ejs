<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('./templates/head'); %>
</head>

<body>
  <!-- header goes here? -->
  <div class="container max-w-full mx-auto sm:px-4">
    <div class="flex flex-wrap">
      <%- include('./templates/sidebar'); %>
      <div class="pl-4 pr-4 sm:w-4/5">
        <h1><%= title %></h1>
        <a href="<%= author.link_to_blog %>" target="_blank">Link to blog</a></p>

        <p>Recipes:</p>
        <dl>
          <% if (author_recipes.length > 0) { %>
          <% author_recipes.forEach(function(recipe) { %>
          <dt><a href="<%= recipe.url %>"><%= recipe.title %></a></dt>
          <dd><%= recipe.description %></dd>
          <% }); %>
          <% } else { %>
          <p>This author has no recipes.</p>
          <% } %>
        </dl>
        <p>
          <a href="<%= author.url %>/delete">Delete author</a>
        </p>
        <p>
          <a href="<%= author.url %>/update">Update author</a>
        </p>
      </div>
    </div>

  </div>
  <footer>
    <%- include('./templates/footer') %>
  </footer>
  <!-- <script src="/tw-elements/dist/js/tw-elements.umd.min.js" type="text/javascript"></script> -->
</body>

</html>